{
    "verbose": true,
    "watch": ["src"],
    "ext": "ts,json,.js",
    "ignore": ["src/**/*.spec.ts"],
    "exec": "npx ts-node ./src/index.ts",
"events":{
    "crash": "sh -c 'lsof -i :${PORT: -900} -t | xargs kill'"
},
"delay": "1000"
}